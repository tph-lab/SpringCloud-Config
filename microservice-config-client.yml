spring:
   profiles:
      active:
        - dev
---
server: 
  port: 8201

spring:
  profiles: dev
  application:
      name: microconfig-test-client

# eureka:
#  client:
#   service-url:
#     defaultZone: http://admin:a@localhost:7001/eureka
info:
   app.name: microservice-config-dev-updated
   company.name: yc
---
server:
  port: 8102

spring:
  profiles: beta
  application:
      name: microconfig-test-client

# eureka:
#  client:
#   service-url:
#     defaultZone: http://admin:a@localhost:7001/eureka
